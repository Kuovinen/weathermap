export default function Header() {
  return (
    <header className="container-fluid bg-info fs-1 text link-light">
      WEATHER
      <div className="fs-4">
        using Open-Meteo weather API, Geonames.org webservices API and
        GoogleMaps API
      </div>
    </header>
  );
}
